<h1>My Articles:</h1>
<ul>


  <% @articles.each do |article| %>
  <div class="article">
    <li><span class="title"><%= link_to article.title, article_path(article) %></span>: <%= article.content %></li>
  </div>
  <% end %>
</ul>

<%= link_to "Create New Article", new_article_path %>
